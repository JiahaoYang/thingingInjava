1)寄存器，处理器内部，无法直接控制
2）堆栈，通用RAM，通过堆栈指针控制。创建程序时，必须确切知道存储在堆栈中项的生命周期，灵活性差，对象引用存储于此
3）堆，也位于RAM区，分配与清理需要更多时间（一种复杂的数据结构，用隐式空闲链表等实现，碎片整理）
4）静态存储器（ROM），存储不变对象（String类型）
5）非RAM存储，流对象（转化为字节流发送给其他机器）和持久化对象（存放于磁盘），可随时恢复为基于RAM的对象

对象作用域 {
	String s = "ac";
}
作用域外引用s无法访问，但String对象仍然占用内存



